# Issues noticed in passing

- Escape does not dismiss the new-node palette on the first press. The search
  field holds focus when the palette opens, so the first Escape blurs the
  field and a second is needed to close the popup.
