scrolview on newnode popup

cleanup binary caches

file name as argument clap

autoupdate image review and autorun node

stacking frames (outer, inner, reference)

auto chosen reference frame for stacking and manually specified

visual node groups

audit allocations overshoot , see where reserve_exact would be better

invert removed retain logic to optimize

mesh once registrations

Darkroom currently recompiles on every run, so each new compile still needs installation

save caches on cache click

imageview to load only when active, until then save to disk

uitest harness internals/test cfg

A Pure node with an unset path input now errors per run. DataType::FsPath's default value is String::new(), and metadata("") is NotFound.

fn hash_bound_fs_path(&self, hasher: &mut DigestHasher, addr: &OutputAddr) -> Option<()> - what is option

staticvalue -> constvalue

GestureSlot - remove?

StepSignals

hits - remove?

remove Gestures

pub(crate) enum Refusal { - remove

fn drain_intents(&mut self, open: &mut OpenDocument) { - ugly

bake output types into wildcard outputs

refactor consumers
